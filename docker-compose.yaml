services:
  app:
    build: .
    container_name: ytplaylistanalyser
    command: gunicorn --reload -b 0.0.0.0:80 src.main:app
    ports:
      - "80:80"
    env_file:
      - .env
    volumes:
      - .:/code
